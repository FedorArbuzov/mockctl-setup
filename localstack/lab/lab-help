#!/bin/sh
cat <<'EOF'
aws-terraform lab container
  /aws-labs          host folder ~/aws-labs (same files, same state)
  localhost:4566     LocalStack (proxied — same URLs as on the host)

  terraform version
  aws --endpoint-url=http://localhost:4566 s3 ls
  verify-final --bucket NAME --lambda NAME [--invoke]

Leave: exit
EOF
